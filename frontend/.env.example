# API the frontend talks to.
# - In dev: leave default in .env.development (http://localhost:8000) — the FastAPI backend.
# - In prod: set this to whichever backend you've deployed (FastAPI or Cloudflare Worker).
VITE_API_URL=http://localhost:8000
