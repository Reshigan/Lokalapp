[project]
name = "lokal-api"
version = "1.0.0"
description = "Lokal Platform API - Digital wallet and services platform"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk"
]
